# CLAUDE.md

函数轨道：面向初中生的函数图像解谜手机网页游戏。玩家调整函数参数，让小球沿函数图像吃到星星、避开障碍、到达终点。

## 常用命令

```bash
node test.js                          # 关卡校验（改关卡后必跑）
python3 -m http.server 8000           # 本地服务器，手机同 Wi-Fi 访问 http://<本机IP>:8000
ipconfig getifaddr en0                # 查本机局域网 IP
```

也可以直接用浏览器打开 `index.html`，不需要构建。

## 文件结构

- `index.html`：页面结构和全部 CSS（内联）
- `game.js`：全部游戏代码，从上到下依次是
  - 常量（`VIEW`、`BALL_R`、`STAR_R`、`GOAL_TOL` 等，单位都是坐标单位）
  - `TYPES`：函数类型（`linear`、`quadratic`），包括参数范围和步长、求值函数 `f`、公式显示 `format`
  - `LEVELS`：关卡数据
  - `simulate()`：纯函数，判定一次发射的结果，不依赖 DOM
  - `initUI()`：界面、Canvas 绘制、动画，只在浏览器里执行
  - 文件末尾用 `module.exports` 导出，供 `test.js` 在 node 里使用
- `test.js`：对每一关穷举所有参数组合，检查参考解能过关、初始值不能过关、固定参数没被改动，并列出全部可行解

## 关卡数据格式

```js
{
  type: 'linear',                  // TYPES 里的键
  title, hint, learn,              // 标题；关卡提示；过关后展示的知识点
  start: -3,                       // 小球出发的 x
  goal: { x: 3, y: 1 },            // 小球走到 goal.x 为止，此时 y 要在 goal.y ± GOAL_TOL 内
  stars: [{ x, y }],               // 必须全部吃到
  walls: [{ x1, x2, y1, y2 }],     // 矩形障碍
  init: { k: 1, b: 0 },            // 初始参数（必须包含该类型的全部参数）
  locked: ['k'],                   // 本关固定不可调的参数
  solution: { k: 1, b: -2 },       // 参考解，test.js 会验证
}
```

过关条件：小球从 `start` 走到 `goal.x` 的整个过程中不飞出画面（|y| ≤ 6）、不撞墙，吃到全部星星，并且终点位置正确。

## 约定

- **改动关卡后必须跑 `node test.js`**。要求参考解能过关、初始值不能过关；原则上每关只有唯一解，有多解时要是有意设计的。
- 坐标点、参数都取在步长的整数倍上（k 为 0.5、b 为 1、a 为 0.25、h/k 为 0.5），避免出现"差一点"的判定。
- 界面文字用中文，面向初中生，表述要通俗。数字显示统一用 `num()`，负号使用 `−`（U+2212）。
- 保持零依赖、零构建：不引入框架和打包工具。等代码量明显变大再考虑拆分文件或迁移到 TypeScript。
- 移动端优先：页面在 320px 宽度下也要能正常使用；新增的可点击控件不小于 36px。
- 进度存在 `localStorage` 的 `fg-cleared` 键里（已通关的关卡数），所有读写都要包在 try/catch 里。

## 内容规则（教育产品）

- 可以参照教材的章节名称和目录结构，但**不能照搬教材原文、例题和插图**，知识点讲解要自己写。
- AI 生成的知识点讲解和题目都要标记为待审核，**上线前必须由数学老师审核**。
- 数学术语以用户提供的教材用语为准（比如初中教材对 k 的叫法），不要擅自引入高中术语。

## 规划（未完成）

目标是按学校教材的章节组织关卡，每一节包括"通俗知识点介绍 + 若干关卡"。

- 数据结构：知识点和关卡只做一套，每个教材版本单独维护一份"章节 → 知识点/关卡"的映射表。
- 这个玩法适合的内容：平面直角坐标系、一次函数、二元一次方程组（两直线交点）、一元一次不等式（看图像）、反比例函数、二次函数。其他章节需要另外设计玩法。
- **待用户提供**：教材版本（2024 新版教材正在逐年替换，今年轮到九年级）、目标册的目录、学校教学进度表、使用场景（学生自学 / 课堂 / 班级布置）、负责审核的老师。
- 不要凭记忆写教材的章节号，要等用户提供目录后再写。

## 已知问题

- 星星在 y 轴上时，会和 y 轴的刻度数字重叠。
- 第 2 关标题和讲解用了"斜率"，初中教材一般不正式使用这个词，要等拿到教材后统一用语。
- 用无头 Chrome 截图时，requestAnimationFrame 动画跑不完，所以过关流程要在真机或普通浏览器里验证。
